# Overridable with -Dkey=value or environment variables (KEY with dots as underscores).
browser=chrome
headless=true

wait.explicit.seconds=15
wait.pageload.seconds=40

the-internet.url=https://the-internet.herokuapp.com
parabank.url=https://parabank.parasoft.com/parabank

# Optional Selenium Grid endpoint
remote.url=
