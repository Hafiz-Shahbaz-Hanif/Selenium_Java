<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.hafiz.automation</groupId>
    <artifactId>selenium-java-testng-framework</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <name>Selenium + Java + TestNG UI Automation Framework</name>
    <description>
        Page Object Model / PageFactory UI automation framework built with Selenium WebDriver,
        Java and TestNG, with parallel execution and ExtentReports reporting.
    </description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <selenium.version>4.35.0</selenium.version>
        <testng.version>7.11.0</testng.version>
        <extentreports.version>5.1.2</extentreports.version>
        <commons.io.version>2.18.0</commons.io.version>
        <slf4j.version>2.0.16</slf4j.version>

        <surefire.version>3.5.2</surefire.version>
        <suite.file>src/test/resources/testng.xml</suite.file>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
        </dependency>

        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
        </dependency>

        <dependency>
            <groupId>com.aventstack</groupId>
            <artifactId>extentreports</artifactId>
            <version>${extentreports.version}</version>
        </dependency>

        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons.io.version}</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <suiteXmlFiles>
                        <suiteXmlFile>${suite.file}</suiteXmlFile>
                    </suiteXmlFiles>
                    <systemPropertyVariables>
                        <headless>${headless}</headless>
                        <browser>${browser}</browser>
                    </systemPropertyVariables>
                    <!-- Hard ceiling so a wedged browser can never hang CI forever. -->
                    <forkedProcessTimeoutInSeconds>2400</forkedProcessTimeoutInSeconds>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>defaults</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <headless>true</headless>
                <browser>chrome</browser>
            </properties>
        </profile>
        <profile>
            <id>smoke</id>
            <properties>
                <suite.file>src/test/resources/smoke.xml</suite.file>
            </properties>
        </profile>
        <profile>
            <id>internet</id>
            <properties>
                <suite.file>src/test/resources/internet.xml</suite.file>
            </properties>
        </profile>
        <profile>
            <id>parabank</id>
            <properties>
                <suite.file>src/test/resources/parabank.xml</suite.file>
            </properties>
        </profile>
    </profiles>
</project>
